import { ComposeForm } from './styles'

const Compose = () => {
  return (
    <ComposeForm>
      <input type="text" name="" id="" placeholder="O que está acontecendo?" />
    </ComposeForm>
  )
}

export default Compose
